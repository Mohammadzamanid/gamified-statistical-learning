# STAGE2_RECONSTRUCTION_BACKLOG.md

One unit per work cycle. A unit is **Complete** only when its commit is on remote `main` and the hashes match.
Status vocabulary is defined in `STAGE2_RECONSTRUCTION_SCOPE.md` §9.

**Stage 2 opened from baseline:** `00f4497ff2f356bd03e3caf3556b3fa6a6de642e`

---

| ID | Work unit | Dependencies | Acceptance criteria | Status | Important files | Tests | Local commit | Remote commit | Push verified | Notes |
|---|---|---|---|---|---|---|---|---|---|---|
| S2-01 | Region-completion achievement repair | Stage 1 baseline | Completed region awards its achievement; incomplete region does not; no duplicates; award survives save/reload; existing tests stay green; new unit + integration tests pass; pushed and verified | **Complete** | `src/core/achievements/engine.ts`, `src/core/curriculum/progress.ts`, `src/core/curriculum/loader.ts`, `src/renderer/state/session.ts`, `src/content/questions/achievements.json` | `tests/unit/achievements.test.ts` (12), `tests/integration/region-completion.test.ts` (6) | `6798b6a` | `6798b6a` | Yes — MATCH | Full SHA `6798b6a71beb3e15ec43e791ca60fa36e2a0c214`; hashes recorded only **after** the push was verified. Suite 73→**90 tests**, 14→**15 files**. Verified the new tests fail (7 failures) against the old stubbed `false`, so they are genuine regression tests. `evaluateAchievements` now *requires* the curriculum, so a caller that omits it fails to compile rather than silently reintroducing the defect. |
| S2-02 | Step-by-step calculation interaction | S2-01 | Multi-step calculation, per-step validation, equivalent numeric formats, per-step hints, misconception classification, retry from failed step, final explanation, mastery update, keyboard accessible; ≥3 real curriculum examples | **Complete** | `src/core/questions/step-calculation.ts` (new), `evaluators.ts`, `normalize.ts`, `types.ts`, `registry.ts`, `src/shared/schemas/question.ts`, `src/core/misconceptions/engine.ts`, `src/core/curriculum/loader.ts`, `src/renderer/components/QuestionRenderers.tsx`, content | `tests/unit/step-calculation.test.ts` (17), `tests/integration/step-calculation-flow.test.ts` (8) | `48bac65` | `48bac65` | Yes — MATCH | Full SHA `48bac65064d45b376d88d92bd775957ecc78105f`; recorded only after the push was verified. Suite 90→**115 tests**, 15→**17 files**. Registry flipped to `implemented: true` only once schema, evaluator, renderer, a11y, and 3 real lesson questions all existed. |
| S2-03 | Point-placement interaction | S2-02 | Number lines, coordinates, graph reading, approximate values; pointer **and** keyboard interaction; configurable tolerance; accessible feedback | **Complete** | `src/core/questions/point-placement.ts` (new), `evaluators.ts`, `normalize.ts`, `types.ts`, `registry.ts`, `src/shared/schemas/question.ts`, `src/core/misconceptions/{engine,detectors}.ts`, `src/core/curriculum/loader.ts`, `QuestionRenderers.tsx`, content | `tests/unit/point-placement.test.ts` (31), `tests/integration/point-placement-flow.test.ts` (9) | `4c4d908` | `4c4d908` | Yes — MATCH | Full SHA `4c4d908bf4395bacbb4c23968fbb86ce46b36fd3`; recorded only after the push was verified. Suite 115→**155 tests**, 17→**19 files**. Keyboard reachability is asserted, not asserted-about: a test steps to every shipped target with `movePoint` alone and submits the result through the real engine. |
| S2-04 | Accessible drag-and-drop | S2-03 | Reusable for sorting, matching, ordering, grouping, simple graph construction; complete keyboard alternative | **Complete** | `src/core/questions/drag-drop.ts` (new), `evaluators.ts`, `normalize.ts`, `types.ts`, `registry.ts`, `src/shared/schemas/question.ts`, `src/core/misconceptions/{engine,detectors}.ts`, `src/core/curriculum/loader.ts`, `QuestionRenderers.tsx`, content | `tests/unit/drag-drop.test.ts` (30), `tests/integration/drag-drop-flow.test.ts` (8) | `70e9527` | `70e9527` | Yes — MATCH | Full SHA `70e95277a0850c67d0af07148d7f0ac7497aab43`; recorded only after the push was verified. Suite 155→**193 tests**, 19→**21 files**. One `placement` primitive covers all five shapes — only the zone configuration differs. Every shipped arrangement is built in a test using **only** the operations the keyboard controls perform, never a drag. |
| S2-05 | Interaction-type audit (all 17) | S2-04 | Per type: schema, renderer, evaluation, correct path, incorrect path, misconception path, keyboard operation, accessible name/instructions, ≥1 genuine curriculum use, save/resume where stateful | **Complete** | `docs/INTERACTION_AUDIT.md` (new), `src/renderer/components/rendered-interactions.ts` (new), `QuestionRenderers.tsx`, `tests/helpers/responses.ts` (new) | `tests/audit/interaction-audit.test.ts` (18) | `acc9bf3` | `acc9bf3` | Yes — MATCH | Full SHA `acc9bf36df47ab97613ec1f1bc77c8355b3cccd1`; recorded only after the push was verified. Suite 193→**211 tests**, 21→**22 files**. The audit is **enforced, not described**: renderer coverage is a shared module the test compares against the registry, and correct/wrong paths are driven from real content for every implemented type. Curriculum use is measured as reachable-from-a-lesson, so demos cannot satisfy it. 5 findings recorded (F-1…F-5), none claimed as fixed. |
| S2-06 | Dedicated spaced-review queue | S2-01 | Due calculation, review screen, overdue items, new-vs-review distinction, mixed-topic review, correct/incorrect rescheduling, persistence, interrupted-session resume, deterministic-clock tests | **Complete** | `src/core/spaced-repetition/review-queue.ts` (new), `src/renderer/state/review-session.ts` (new), `src/renderer/screens/ReviewScreen.tsx` (new), `store.ts`, `App.tsx`, `TopBar.tsx`, `src/shared/schemas/profile.ts`, `constants/app.ts`, `persistence/migrations.ts` | `tests/unit/review-queue.test.ts` (13), `tests/integration/review-session.test.ts` (12) | `e8e0cb2` | `e8e0cb2` | Yes — MATCH | Full SHA `e8e0cb2b8a02ce933671aa8cf4c2a5c727760855`; recorded only after the push was verified. Suite 211→**236 tests**, 22→**24 files**. **Save schema 1 → 2** with a real migration, because resume requires persisting the in-flight queue. The queue is **frozen at start**, so resume shows what it showed before rather than a rebuilt queue. Clock is an argument everywhere — no `Date.now()` inside the review core. |
| S2-07 | Region 1 curriculum architecture | S2-05 | Complete lesson + prerequisite graph; every required topic represented and reachable | **Complete** | `src/content/worlds/curriculum.json`, `src/content/questions/questions.json`, `.github/workflows/ci.yml` | `tests/audit/region1-architecture.test.ts` (10) | `dd39d38` | `dd39d38` | Yes — MATCH | Full SHA `dd39d38c57b4083d7829a698a56960b6ff484c8c`; recorded only after the push was verified. Suite 236→**246 tests**, 24→**25 files**. All **17 scope §2 topics** now have a skill, an objective, a lesson and a seed question, in 4 new modules with a prerequisite graph. **Architecture only** — a `skeleton honesty` check asserts each new lesson still has exactly one question, so none can be mistaken for finished work. Completeness is S2-08. |
| S2-08 | Region 1 lessons and interactions | S2-07 | Every lesson satisfies all 18 structure requirements (scope §5) | **Complete** | `src/shared/schemas/curriculum.ts`, `src/core/curriculum/demonstration.ts` (new), `src/core/misconceptions/detectors.ts`, `src/renderer/components/DemonstrationPanel.tsx` (new), `src/renderer/screens/LessonScreen.tsx`, `src/content/worlds/curriculum.json`, `src/content/questions/{questions,misconceptions,remediations}.json`, `tests/helpers/complete-lessons.ts` (new) | `tests/audit/lesson-structure.test.ts` (24), `tests/unit/demonstration.test.ts` (17), `tests/integration/module1-lessons.test.ts` (12), `tests/audit/region1-architecture.test.ts` (12, was 10) | `629dd74` | `629dd74` | Yes — MATCH | Full SHA `629dd74b763013016ae09a40064be2a976b394d7`; recorded only after the push was verified. **All 17 of the 17 scope §2 Region 1 topic lessons satisfy the 18 requirements**, across four cycles: `629dd74` (Module 1), `2f1ec31` (Module 2), `def8b1d` (Module 3), `615e7c4` (Module 4) — each remote-verified. Suite 246→**365 tests**, 25→**31 files**. Lessons carry a data-driven `demonstration`, a `formalTerm` with every symbol explained, and six practice roles (D-012…D-014, D-017). The audit **drives** each demonstration through the real core module, so a placeholder control fails; beginner safety is enforced by requiring a symbol to be explained by the lesson or a prerequisite. **Named exception:** `l.reading-tallies` and `l.middle-harbor` are Stage 1 lessons inside the Region 1 container teaching Region 2 topics; they are **not** Complete and are re-cut in S2-11. A check asserts they are the only two, so the exception cannot grow. No topic is Complete under scope §4 (7-8 authored questions each, zero generators) — that is S2-09. |
| S2-09 | Region 1 validated content expansion | S2-08 | ≥100 validated interactions per completed major topic, multiple reasoning families, duplicate/near-duplicate gates, misconception mappings, a11y descriptions, machine- and human-readable reports | **Partial** | `src/core/generation/*` (new), `src/content/generators/*` (new: `arithmetic.ts`, `parts.ts`, `ratios.ts`, `position.ts`, `data.ts`), `src/content/generated.ts` (new), `src/content/index.ts`, `scripts/coverage-report.ts` (new), `docs/CONTENT_COVERAGE.md` + `docs/content-coverage.json` (generated), `tests/helpers/complete-topics.ts` (new) | `tests/audit/content-coverage.test.ts` (29, was 21), `tests/unit/generation.test.ts` (19, was 17) | `01f29ad` | `01f29ad` | Yes — MATCH | Full SHA `01f29ad284a7641412447f898f85d328232cbbf0` (cycle 4); recorded only after the push was verified. Cycles 1-3 `0de5fff7d76fc9982f75391da1d6f751d60cdfdb`, `c7f99d5d6c4352567a6d8bbbdc26b6de0addd9c4`, `10685dd01786f92261cafd1fbd951e60177528c2` (all remote-verified). **17 of the 22 curriculum topics meet §4 after cycle 4, and every Region 1 topic is among them** (151-855 available interactions each, 4-8 reasoning families, largest single reasoning shape under 40%). **The unmet criterion is now entirely Region 2**: mean, median, range, choosing measures and data literacy have zero generator families and are reported in `CONTENT_COVERAGE.md` as failures with reasons — they belong to **S2-17**, so S2-09's own Region 1 objective is met and the row stays Partial only because §4 is stated over all 22 topics. Suite 365→**413 tests**; **6,607 validated generated interactions**, merged into `loadPlayableContent()`. **Cycle 1:** the answer check was tautological (D-020). **Cycle 2:** `exactFingerprint` was blind to ordering items (D-024); defect-class rejections were not required to be zero (D-023). **Cycle 3:** the misconception gate had only ever run on its rejecting side; an audit now drives the real engine (D-025), and four dead rejection guards were found by a zero in the report (D-026). **Cycle 4:** that same D-025 defect was then made in the very next module — `mc.digits-mean-numerical` tagged on a choice when its detector reads placements — and caught by the check rather than by care; generated datasets are written out, not computed (D-027). |
| S2-10 | Region 1 boss investigation | S2-09 | Multi-step saveable investigation combining Region 1 skills; awards region achievement; unlocks Region 2 | **Complete** | `src/shared/schemas/curriculum.ts`, `src/shared/schemas/profile.ts`, `src/shared/constants/app.ts`, `src/core/investigations/engine.ts` (new), `src/core/curriculum/{progress,loader}.ts`, `src/core/persistence/migrations.ts`, `src/renderer/state/{session,store}.ts`, `src/renderer/screens/InvestigationScreen.tsx` (new), `src/renderer/screens/RegionScreen.tsx`, `src/renderer/app/App.tsx`, `src/content/worlds/curriculum.json`, `src/content/questions/questions.json`, `tests/helpers/{complete-bosses,investigation-playthrough}.ts` (new) | `tests/unit/investigations.test.ts` (17), `tests/audit/investigation-structure.test.ts` (12), `tests/integration/region-completion.test.ts` (updated) | `dc87d8b` | `dc87d8b` | Yes — MATCH | Full SHA `dc87d8b87c88b8d8b4db0d8cb1c6f20759aec8f4`; recorded only after the push was verified. `inv.r1-harbour-audit` — **The Harbourmaster's Audit** — runs in **5 stages over 15 authored questions**, drawing on **14 of Region 1's 17 skills across all four modules**. Suite 413→**443 tests**, 33→35 files; authored questions 145→160. A step is an ordinary `LessonSession`, so mastery, spaced review, misconception detection and achievements behave identically inside a case and inside a lesson — only the completion record differs (D-028). **Behaviour changed deliberately:** a region is no longer complete, and `ach.harbor-charted` no longer awarded, until the case is closed; four existing tests failed on that and were updated to play the boss rather than relaxed. Save schema **2 → 3** with a migration, so an older save resumes with no case opened rather than one falsely available. Region 2's boss is declared as **owed to S2-18** in `tests/helpers/complete-bosses.ts`, and an audit fails if that debt is quietly paid off or quietly dropped. Eight probes, all reverted, all eight bit on the first run. |
| S2-11 | Region 2 world and curriculum architecture | S2-10 | Distinct world, narrative, module sequence, prerequisite graph, objectives, laboratory unlocks, boss specification | **Complete** | `src/content/worlds/curriculum.json`, `src/content/questions/questions.json`, `src/shared/schemas/curriculum.ts`, `src/core/curriculum/progress.ts`, `src/renderer/screens/LabScreen.tsx`, `tests/helpers/region2-topics.ts` (new), `docs/REGION2_BOSS_SPEC.md` (new) | `tests/audit/region2-architecture.test.ts` (20), `tests/audit/region1-architecture.test.ts` (updated) | `dfb8e4f` | `dfb8e4f` | Yes — MATCH | Full SHA `dfb8e4f7c4c7b78d2ec612c4bb433e68d8f448c3`; recorded only after the push was verified. Region 2 holds **22 topic lessons across 6 modules** — all 21 scope §2 topics plus choosing a measure of centre. **19 are skeletons** seeded here (one question, no demonstration, the S2-07 shape); **3 are Stage 1 lessons re-cut into the region** (`l.reading-tallies`, `l.middle-harbor`, `l.spread-1`), which discharges the re-cut S2-08 recorded as owed to this unit — **Region 1 now holds no un-Complete lesson at all**, and the placeholder modules `m.harbor-1` and `m.atoll-1` are retired. Suite 443→**463 tests**, 35→36 files; authored questions 160→179. **`SkillSchema.stage` is now required with no default** (D-030), which closes the §10 'a new skill has no stage classification' guard and makes 'a Stage 3 topic before Stage 3' checkable. The laboratory gate is declared in the curriculum and enforced by `isLaboratoryUnlocked` (D-032). **Correction:** the module prerequisite graph gated nothing — `isLessonUnlocked` reads lesson prerequisites, so the region opened six doors at once; found by an audit computing availability through the real unlock rule rather than reading the JSON (D-031). `report:coverage` now reads **17 of 40** rather than 17 of 22: the denominator grew because Region 2's 18 topics now exist and are reported as failures with reasons, which §4 requires; their generators are S2-17. Eight probes, all reverted, all eight bit on the first run. |
| S2-12 | Central tendency lessons | S2-11 | Frequency, proportion, percentage, mean, median, mode, measure selection, outlier effects, skew effects; draggable/editable datasets | **Partial** | `src/content/worlds/curriculum.json`, `src/content/questions/{questions,misconceptions,remediations}.json`, `tests/helpers/{complete-lessons,staged-inherited}.ts`, `tests/integration/region2-lessons.test.ts` | `tests/audit/lesson-structure.test.ts` (26), `tests/audit/region2-architecture.test.ts` (28), `tests/integration/region2-lessons.test.ts` (31) | `08f1c37` | `08f1c37` | Yes — MATCH | Full SHA `08f1c371038650f51bb4b48d19bd194122e11dbf` (cycle 4); cycle 3 `aa48c7c16f48fd4372b27f52681798da0e31054f`; cycle 2 `34a63f97a570d9c6c6f3a65f9be6ed9969162715`; cycle 1 `80657919a356d2370965f9bcd8cdd0c069fc3b2d`. **All 9 of 9 lessons Complete** — `m.r2-counts` and `m.r2-centre` entire, plus outliers and skew from `m.r2-variation`. **One criterion remains unmet and is not claimed: draggable/editable datasets** — the demonstrations are control-driven, which meets requirement 4; editable datasets are S2-15, which is why this unit stays Partial rather than Complete. Variance and standard deviation sit ahead of outliers and skew in the prerequisite chain and belong to S2-13, so `m.r2-variation` is deliberately half-written and these two lessons are finished but not yet reachable. Suite 505 tests; authored questions 212 → 222; misconceptions 26 → 27; remediations 25 → 26. Cycle 4's finding: **nine Region 2 lessons had been declared Complete across three cycles without anything ever playing one** — the audits read content, nothing ran it (D-037). The new playthrough was then probed with a deliberately wrong mastery answer and **failed nothing**, because responses are built from each question's own declared answer; the test's header now states exactly what it proves rather than the stronger claim its first draft made (D-038). Ten probes, of which nine bite and the tenth is the finding. |
| S2-13 | Spread and position lessons | S2-12 | Min/max, range, quartiles, percentiles, IQR, variance intuition, standard deviation, distribution comparison | **Partial** | `src/content/worlds/curriculum.json`, `src/content/questions/{questions,misconceptions,remediations}.json`, `tests/helpers/complete-lessons.ts` | `tests/audit/lesson-structure.test.ts` (26), `tests/integration/region2-lessons.test.ts` (37) | `cf88cd3` | `cf88cd3` | Yes — MATCH | Full SHA `cf88cd3bb8dace8b5ec1049b9a296ffdc2f7622d` (cycle 1). **2 of 6 lessons Complete**: `l.spread-1` (range, the third and last inherited Stage 1 lesson in Region 2 — it carried one question, which teaches range, so it needed no redistribution) and `l.r2-quartiles`. Percentiles, IQR, variance and standard deviation remain; **SD must be built as distances → squared distances → their average → square root**. **Criterion 5, distribution comparison, has no lesson in Region 2's declared topic list at all** — meeting it needs a new lesson and topic or an explicit decision that the boss carries it, recorded rather than dropped. Suite 511 tests; authored questions 222 → 232; misconceptions 27 → 29; remediations 26 → 28. Two findings. **The beginner-safety guard could not see the Unicode minus sign**, which Stage 1 content had used since the baseline: `q.range-tides` wrote 3.4 − 1.2 with U+2212 while its chain explains ASCII `-`, and the operator class listed only the ASCII form (D-039). **S2-12's report and handoff both claimed the outlier and skew lessons were 'not yet reachable' and told this unit to reorder around it; driving a save through the real unlock rule shows the whole chain unlocking today** — a seeded lesson is answerable, so it completes, so it opens the next (D-040). Eight probes, all bite, including a paired probe proving the widened operator class is what does the work. |
| S2-14 | Data-visualization lessons | S2-13 | Bar charts, histograms, dot plots, box plots, scatterplots, graph selection, truncated axes, bin-width effects, misleading framing | Not started | `src/renderer/components/*`, `src/content/*` | pending | — | — | — | Every chart needs an accessible textual equivalent. |
| S2-15 | Descriptive statistics laboratory | S2-14 | Create/edit datasets, add/remove values, sort, add outliers, compare two datasets, change graph type, change bins, live statistics, save/reload experiments, reset, export summaries, accessible text descriptions | Not started | `src/renderer/screens/LabScreen.tsx` | pending | — | — | — | Must be a learning environment, not a calculator. Replaces Stage 1 known defect #3 placeholders. |
| S2-16 | Region 2 misconception library | S2-15 | ≥12 named misconceptions reachable, each remediation with all 9 required parts | Not started | `src/content/questions/*` | pending | — | — | — | Includes reverse validation: no undeclared distractor misconceptions, no orphaned remediations, no count-inflating tags. |
| S2-17 | Region 2 validated content expansion | S2-16 | ≥100 validated interactions per completed Region 2 major topic, diverse reasoning families | Not started | generators + report tooling | pending | — | — | — | |
| S2-18 | Region 2 boss investigation | S2-17 | Dataset inspection, summary selection, graph selection, misleading-presentation detection, outlier reasoning, distribution comparison, evidence-based conclusion; save/resume, adaptive support, remediation, achievement, Stage 2 completion | Not started | `src/content/*`, `src/renderer/screens/*` | pending | — | — | — | |
| S2-19 | Save, resume, and recovery audit | S2-18 | Interruption at lesson, multi-step calculation, boss, review queue, laboratory; after achievement award, mastery update, settings change; multiple profiles, atomic writes, backup rotation, corrupt-primary recovery, migration, invalid import, missing save, duplicate-achievement prevention, review-schedule and laboratory-state persistence | Not started | `src/core/persistence/*` | pending | — | — | — | Duplicate-achievement prevention already covered for regions by S2-01; extend to all award kinds. |
| S2-20 | Accessibility harness and audit | S2-19 | Real DOM-capable a11y testing added to `package.json` and CI; all checks in scope §6 | Not started | `package.json`, `.github/workflows/ci.yml`, `tests/a11y/*` | pending | — | — | — | **`test:a11y` may only be referenced once the script exists.** CI currently omits it deliberately. |
| S2-21 | Stage 2 closure audit | all above | Full validation suite, measured totals, fresh-save playthrough, save/resume audit, laboratory audit, remote persistence audit, closure drift guards enforced | Not started | `docs/*` | pending | — | — | — | Stage not closed until final commit pushed + verified, docs pushed, tree clean, `stage-2-complete` tag pushed **or explicitly marked blocked**, and source ZIP + Git bundle exported outside history. |

## Measured totals (updated as units complete)

Measured from this repository — never recalled from earlier reports.

| Metric | At Stage 1 baseline | Current |
|---|---|---|
| Test files | 14 | **33** |
| Tests | 73 | **403** |
| Regions | 2 | 2 |
| Modules | 2 | **6** |
| Skills | 6 | **23** |
| Objectives | 6 | **23** |
| Lessons | 3 | **20** |
| Authored questions | 14 | **145** |
| Validated generated interactions | 0 | **3,192** |
| Topics meeting scope §4 | 0 | **5 of 22** |
| Misconceptions | 8 | **24** |
| Remediations | 7 | **23** |
| Achievements | 4 | **6** |
| Datasets | 1 | 1 |
| Interaction types implemented | 11 of 17 | **14 of 17** |
| Interaction types used by content | 11 | **14** |
| Renderer bundle | 285.73 kB (83.82 kB gzip) | **538.47 kB (143.72 kB gzip)** |

Still stubbed (3): `formula-construction`, `simulation-prediction`, `confidence-rating`.

**Region 1: 17 of 17 required topics represented, and all 17 lessons are Complete** to the 18 structure
requirements. No skeletons remain. Two Stage 1 lessons inside the Region 1 container (`l.reading-tallies`,
`l.middle-harbor`) teach Region 2 topics and are deliberately not upgraded — see the S2-08 row and S2-11.

**Save schema version: 2** (was 1 at the Stage 1 baseline). Migration `1 -> 2` adds `reviewSession`.

**Interaction-count rules note.** Authored records and validated available interactions are different numbers and are
counted separately. As of S2-09 cycle 4, **17 of 22 topics** meet the ≥100 threshold in
`STAGE2_RECONSTRUCTION_SCOPE.md` §4, and **every Region 1 topic is among them**. The 5 that do not are the whole Region
2 inheritance — mean, median, range, choosing measures, data literacy — which have zero generator families, appear in
`docs/CONTENT_COVERAGE.md` as failures with reasons, and belong to **S2-17**. Lesson completeness (§5) and topic
completeness (§4) remain different claims, and they are now both closed for Region 1: all 17 Region 1 lessons are
Complete under §5, and all 17 Region 1 topics are Complete under §4.

Windows runtime status: **unverified**, unchanged. `package:win` is configured but has never been compiled or
smoke-tested; reconstruction runs on Linux. No Windows claim is made.
